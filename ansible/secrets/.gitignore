# Whitelist only the recipients list and the age-encrypted vault passwords, so a
# plaintext password (or any other stray) can never be committed here.
*
!.gitignore
!age-recipients.txt
!vault-pass.age
