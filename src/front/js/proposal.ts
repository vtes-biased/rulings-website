// Proposal page: hub (list/switch/start) + diff review. Lifecycle buttons and inline name/desc
// autosave live in the shared chrome bundle (setupProposal), so this is a thin entry.
import "./chrome.js"
